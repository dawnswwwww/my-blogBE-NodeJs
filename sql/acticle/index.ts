const saveArticle = require('./saveArticle')
const queryList = require('./queryList')
const queryDetail = require('./queryDetail')

module.exports = {
    saveArticle,
    queryList,
    queryDetail
}